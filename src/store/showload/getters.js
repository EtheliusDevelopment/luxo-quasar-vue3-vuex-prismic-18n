/*
export function someGetter (state) {
}
*/

export const getBool = state => state.load;

// export const getUser = (state, index) => state.users[index];

// export const getCards = state => state.user.cards;
